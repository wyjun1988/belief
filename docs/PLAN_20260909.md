# 다음 단계 계획 (2026-09-09) — M2 · RTX PRO 6000

기준 표(115채, 유령 제외, 무GT): **전체 0.777 · ① 0.813 · ② 0.615(RoI 0.688) · ③ 확인기회O 9건·인계 4·정답 0.000**.
근거는 `docs/RESULTS_OWL_20260819.md` §166-25(유령)·§166-29(앵커 접음)·§166-30(임계)·§166-31(OG 12채)·§166-32(사다리·인스턴스 선택).

## 0. 무엇이 정해졌나 (다시 열지 않는다)
| 갈래 | 판정 | 근거 |
|---|---|---|
| 학습식 스캔 포즈(LoGeR·CUT3R·VGGT·LingBot) | **닫음** | 무GT 정렬 상한 0.54 (§RTX 9-3) |
| ③ 앵커 문맥(RETR=anchor) | **닫음** | 커버리지↔정밀도 정면 교환, 기준 미달 0.772<0.777 (§166-29) |
| 검출 임계 0.06 | HSSD 중립·OG 채택 | §166-30 · RTX 9-10 |
| VLM 인스턴스 진위(개별 yes/no) | 무효 | RTX 9-11 |
| 검출기 | OWLv2-base 확정 | RTX 9-1 |

## 0-1. 사용자 지적 반영 (2026-09-09 11:20 · §166-33)
"기록 레버는 ① 만 올린 것 아니냐" — **맞다.** 행 단위로 기록 GT 는 ① +122건 · ② **−2건** · ③ 0건이다. ② 에서 진 2건은
기록 오류가 우연히 목적지를 가리켰던 운이고, 정직한 ② 는 0.615 가 아니라 **0.538**이다. 전체 행의 93% 가 ① 이라 **평균 점수는
사실상 ① 점수**다 → 앞으로 보고는 ①/②/③ 를 따로 적고, 평균은 참고로만 쓴다.
그리고 ③ 는 부재 검출이 아니라 **belief(어디로 갔나)** 에서 막혀 있다: 정답 방이 belief 순위 중앙 8위/12, 1위 적중 0/21.
아래 P0 은 "헤드라인·① 개선", P-③ 는 "기여의 핵심"으로 분리한다.

## 1. P0 — 초기맵 **인스턴스 선택** (M2, ① 전용·헤드라인)
0.777 → 0.939 의 전부가 여기 있다(§166-32). 검출은 이미 충분하고(후보 없음 0.2%), 선택만 0.729 → 상한 0.874.
- **A. 후보 비교 질의(핵심)**: 타입당 후보 2.9개의 스캔 크롭을 한 번에 주고 "이 집의 <타입>은 어느 것인가"를 묻는다. 개별 yes/no(9-11 실패)가 아니라 **택1 비교**. 크롭 파이프라인은 있다(`anchor_crops.py`). 4B mlx 로 먼저, 되면 RTX 9B/큰 모델로 비교.
- **B. 중복 군집 병합**: 같은 물체가 깊이 오차로 여러 군집이 되는 경우 — CLIP 외형 유사 + 카메라 방위 일치면 병합(후보 2.9 → 감소 = 선택 문제 자체를 줄인다).
- **C. 채점**: `diag_initmap_pos.py` 의 방 정답(현재 0.729)으로 먼저 재고, 이긴 규칙만 벤치.
- 성공 기준: ① 방 정답 ≥0.80 (벤치 전체 ≥0.85 기대).

## 2. P1 — 깨끗한 데이터 재생성 + ③ 표본 (M2, 하룻밤)
- 유령 수정판 생성기로 115채 재생성. 지금 표는 "유령을 옮긴 54 에피소드"에서 가구가 실제로 옮겨진 오염이 남아 있다.
- 동시에 **③ 표본 확대**: 현재 확인기회O 9건으로는 어떤 개선도 유의하지 않다. 집당 ③ 2~3건 + 확인 방문 보장(`--c3-check-dist 1.2`), 목표 30건 이상.
- 생성 ~3분/집 + 체인. `phantom_census.py` 로 새 데이터의 유령 0 확인.

## 3. P2 — ② 채택 커버리지 (M2, P0 뒤)
채택률 0.46 · 채택정답 0.83 → 늘리는 쪽이 맞다. 단 인식 GT 사다리가 ① 을 깎았으므로(0.991→0.889) **게이트를 풀되 확인을 붙인다**: 질의 시점 검출 임계 ↓ + 검증기 크롭 확인 통과분만 채택.

## 4. P-③ — belief 재설계 (M2, **기여의 핵심**)
③ 는 표본만 문제가 아니었다(§166-33). 세 갈래를 순서대로:
1. **지표부터 고친다**: 방 하나 정확히 맞히기(현행) 외에 **top-3 적중·정답 방의 belief 순위**를 같이 보고한다. 지금은 개선해도 0.000 이라 관측이 안 된다.
2. **규약 정합**: 평가기 belief 의 후보집합을 시나리오와 같게(현재 방·실외·통로 제외) 하고, 모르는 방 유형 기본값을 생성기(0.02)와 평가기(0.25) 중 하나로 통일한다. 이것만으로 3위 이내 2 → 4, 5위 이내 5 → 10.
3. **증거 기반 belief**: 유형 사전확률만으로는 같은 유형 방 인스턴스를 못 가른다. 1fps 에서 얻는 무GT 신호를 더한다 — (a) 사라진 시각 이후 사람이 오래 머문 방, (b) 그 방에서 같은 유형 물체가 새로 검출됐는지, (c) 물체를 들고 이동한 구간(연속 프레임에서 물체가 카메라와 함께 움직임).
4. 확인기회 자체는 `ABS_ROOMGATE` 로 9 → 19 까지 늘릴 수 있다(① −0.02 대가). P1 로 표본을 키운 뒤 균형점을 다시 고른다.

## 5. RTX PRO 6000 배정
| 번호 | 과제 | 지금 할 수 있나 |
|---|---|---|
| **9-13** | OG 12채 ③ 14행(house·oid·type·branch·record·ans·tgt·ok) 회신 — ③ 0.214 가 belief 정답인지 미인계 기록 정답인지 가른다 | 즉시(5분) |
| **9-14** | OG 12채 **기록 GT 사다리**(M2 결과상 ① 전용 이득으로 예상 — OG 도 같은지 확인): `INITMAP_FILE=initmap_gt.json` 로 벤치만. M2 의 0.939 에 대응하는 값이 OG 에도 나오는지 = 같은 병목인지 확인 | 즉시(30분) |
| **9-15** | P0-A(후보 비교 질의)의 OG 판 — M2 가 코드를 넘긴 뒤. 큰 VLM(Qwen 9B/72B)이 4B 보다 나은지도 같이 잰다 | M2 대기 |
| **9-16** | OG 생성 확장: ③ 중심 시나리오로 12→24채 | M2 가 시나리오 파라미터 넘긴 뒤 |
규칙 불변: 코드는 M2 에서 쓰고 푸시, RTX 는 실행·보고. GPU0 는 건드리지 않는다.


## 6. 2026-09-09 밤 갱신
- 파일럿 12채 통과(사슬 OK·유령 0). **③ 대본은 far_low 로 되돌림**(사전확률 대본은 ③ 18건 중 15건이 ② 로 샘, §166-37). belief 는 우리 점수가 아니므로 대본이 belief 에 유리할 필요가 없다(§166-36).
- **121채 본 실행 보류**: 파일럿의 ③ 확인기회가 0/9 — 옛 자리를 향한 프레임의 PnP 등록이 병목. 임계 완화는 HSSD 에서 포즈를 망친다. 다음: 자리 기준 참조 프레임 검색(스캔 중 그 자리를 향한 프레임만 후보) + 근접 패스. 이게 파일럿에서 확인기회를 만들면 121채는 RTX 로.
- 새 시뮬레이터: 규약·파이프라인 확인 완료, ③ 인계 경로 동작(1/1). 성능은 라벨·표본 문제(`docs/NEWSIM_20260909.md` §3·§4).
- 확정된 기본값: 부재 인계 이동성 게이트 `ABS_MOB_ALL=0.1`(① 거짓 인계 10→0) · 새 시뮬레이터 PnP top20·20 / HSSD 는 top5·50 유지.
