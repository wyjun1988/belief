# 새 시뮬레이터 5차분 요청 (2026-09-19)

4차분 잘 받았습니다. **요청 3건이 모두 반영됐고 전부 확인했습니다.** `scenes/` 동봉(해시 일치), `static_queries.json` 으로 ① 질의 확대(에피소드당 33/24건), 유일 범주 제한입니다. 특히 `static_queries.json` 은 저희가 직접 고르려던 일을 대신해 주신 것이라 큰 도움이 됐습니다.

## 4차분으로 잰 결과

| | 값 |
|---|---|
| ① 안 움직인 물체 정답률 | 0.576 (n=**295**) |
| ① 오판(안 움직였는데 움직였다고 판단) | **0.000** |
| ② 이동 후 재촬영 | 0.273 (n=11) |
| ③ 부재 → 인계 | 측정 불가 (n=0) |

**① 은 이제 제대로 측정됩니다.** 3차분은 에피소드당 1건이라 오판율을 아예 못 쟀는데, 4차분은 295건이고 오판이 한 건도 없습니다.

## 요청 1 (가장 중요) — 에피소드 수를 늘려 주세요

**② 와 ③ 은 아직 못 잽니다.** 에피소드당 이동 1건·부재 1건이라 10 에피소드면 각 10건입니다. 신뢰구간 ±0.05 로 재려면 **각 370건 안팎**이 필요합니다.

| 지표 | 4차분 | 필요 | 
|---|---|---|
| ① 오판율 | 295 | 246 ✓ |
| ② 채택률 | **11** | 385 |
| ③ 인계율 | **10** | 369 |

두 가지 방법이 있고, **둘 다 좋습니다**.

- **(a) 에피소드를 늘린다** — 장면 5~10 × 에피소드 각 10개(총 50~100). ②·③ 이 각 50~100건이 됩니다.
- **(b) 한 에피소드에 사건을 더 넣는다** — 지금은 이동 1·부재 1·불변 1 인데, **이동 3~5건·부재 3~5건**으로 늘리면 에피소드 수를 안 늘려도 됩니다. 5분 영상에 사건 6~10개는 부자연스럽지 않습니다.

(b) 가 저희 쪽 처리 비용이 훨씬 적습니다. 가능하면 (b) 를 우선, 그 다음 (a) 로 규모를 키워 주시면 좋겠습니다.

## 요청 2 — 스캔이 사람이 다니는 곳을 지나가게

저희는 스캔 영상으로 3D 지도를 만들고 거기에 라이브 프레임을 맞춰 위치를 찾습니다. 4차분에서 **라이브 프레임의 28%만 지도에 붙었습니다**(기존 HSSD 데이터는 60%).

원인 하나는 스캔 위치와 사람의 동선이 떨어져 있는 것입니다. 라이브 카메라에서 가장 가까운 스캔 지점까지 거리가 **중앙 0.26~0.99 m**(집마다 다름)인데, 잘 되는 데이터는 **0.16 m** 입니다. 한 집(house_0004)은 0.99 m 였고 등록률이 0.04 로 최악이었습니다.

**스캔 경로가 에피소드의 보행 동선을 한 번씩 지나가도록** 잡아 주시면 좋겠습니다. 스캔 길이를 늘리실 필요는 없습니다.

## 요청 3 — 참고용 (필수 아님)

라이브 영상의 **16%가 흐릿합니다**(선명도 지표 기준). 스캔 영상은 0% 입니다. 선명한 지도에 흐린 질의를 맞추는 셈이라 정합이 어렵습니다. 모션블러 설정을 낮출 수 있으면 도움이 되지만, 실제 환경에서도 흔한 일이라 **필수는 아닙니다**.

## 그대로 유지해 주실 것
- `scenes/<장면>/scene_graph.json` 동봉
- `static_queries.json` (형식·정책 그대로)
- `initial_state.json` 의 `queryable_initial_state`
- 장면당 스캔 1개, 지금 길이(4,300~4,700 프레임) 그대로


---
## 추가 (2026-09-21) — 3장면 15에피소드 판 수령 후

받은 판 확인했습니다. vol8_03 추가·장면그래프 동봉·영상 무결성 모두 좋습니다. 두 가지만 더 부탁드립니다.

**1. (재요청) 에피소드당 이동·부재 사건을 3~5건으로.** 이번 판도 에피소드당 1건씩이라 ②③ 표본이 15건입니다. 위 §요청 1 그대로입니다.

**2. (신규) `static_queries.json` 의 각 항목에 `movable` 플래그.** 저희 평가는 "움직일 수 있는 물건" 만 대상으로 합니다(사용자는 냉장고가 아니라 노트북을 찾습니다). 이번 정지 질의 445건 중 **95건(21%)이 소파·냉장고·벽난로·침대·가스레인지** 같은 붙박이였습니다. 저희가 임시로 범주별 표를 만들어 거르고 있는데, 생성 쪽에서 `"movable": true/false`(또는 `mobility: 0~1`)를 붙여 주시면 정확합니다. 기준은 "사람이 한 손으로 들어 옮길 수 있나" 정도면 됩니다.

참고: `visual_review.json` 이 `not_reviewed` 로 와 있습니다. 검수 뒤 바뀌는 게 있으면 같은 이름으로 다시 주시면 됩니다.
