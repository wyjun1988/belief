# RTX PRO 6000 작업 지시 (2026-09-03) — `git pull` 후 이 순서대로

repo 상태: `scripts/og_episode.py` = 너희 v3 본체 + v4 델타 + 아이맥 yaw 변환(§11) 이다.
(v2 위 손병합본은 폐기하고 v3 본체를 통째로 받은 뒤 다시 얹었다 — `smooth_program` 포함.)

## 1. 보행 사실성 (SPEC 4-4) 을 og_episode 에 넣는다 — 20채 생성 **전**
검수 영상 지적: "한 사람이 같은 공간을 쉬지 않고 계속 돌고, 너무 빠르다."
- 목적지 도착 후 **체류**: 방 체류 가중에 비례한 길이(평균 30~40 프레임, 지수분포),
  제자리에서 yaw 사인 스캔 ±35°. 체류 프레임 비율 ≥ 0.5.
- 이동 0.25 m/프레임 이하, 회전 평활 0.3~0.4, **프레임당 회전 ≤ 25°** (이음새 포함).
- 방 전환 ≤ 25회/1200프레임. 연속 같은 방 재방문 금지.
- `audit.json` 에 `moving_frame_frac`·`room_switches`·`mean_dwell_frames` 를 넣고 게이트로:
  `moving_frame_frac ≤ 0.5`, `room_switches ≤ 25·(frames/1200)`.
- 참고 구현: `scripts/hab_episode.py` 의 `--dwell/--pace/--turn/--scan/--max-turn`
  (1장면 실측 dwell30: 정지 0.52 · 0.26 m/f · 회전 중앙 1.1°).
- 검수 영상 캡션에 "1fps 원본 10fps 재생 = 10배속" 명시.

## 2. 짧은 판으로 확인 (600~1200 프레임, Rs_int seed 0)
확인할 5줄: `EPISODE_OK` · 위 보행 게이트 3개 · `yaw_audit_ours.median_abs_error_deg ≈ 0`
(0.5° 게이트) · `gen_selfcheck` geo_ok True (수납 이동은 hidden_verified 로 통과하도록
규칙 바꿨음) · case2 ≥ 5 (`evidence.closest_m` ≤ 1.8).

## 3. 20채 생성
- 씬·seed: `scripts/og_scene_screen.py` 로 확정 (실외 방 씬 제외, 방 4~8·주방·화장실).
  Rs_int·Pomaria 등 3~5씬 × seed. Pomaria 처럼 얇은 씬은 케이스 목표 +2 마진.
- `--case4`: 범위 밖 방이 있는 씬만 >0 (선택지 1). 나머지 0.
- 소품 주입 유지 (`gt0[].injected` 플래그).

## 4. 생성 후 그 자리에서 평가 체인 (프레임이 거기 있으니 RTX 에서)
```bash
SKIP_GEN=1 OUT=<og20 경로> BENCH_DIR=~/khcache/bench-og1 SCENES=<사용 안 함> \
  PY=~/kx-venv/bin/python SCORER=hf CALIB=1 bash scripts/bench_v2_chain.sh > bench_og1.log 2>&1
```
(`SKIP_GEN=1` 이면 SCENES 는 안 읽는다. HSSD_DATASET 검사가 걸리면 그 줄은 OG 에서 무시해도
됨 — 필요하면 `[ -f "$HSSD_DATASET" ]` 검사를 `SKIP_GEN` 일 때 건너뛰게 고쳐서 커밋.)
캘리브(CALIB=1)는 9B 채점기라 필수. 보고: 자가검사 20줄 + `=== bench-v2 run0 ===` 블록
전체(재료 사다리·3경우·기준선·CI·증거 조건부) + `캘리브 →` 한 줄.

## 5. (여유 시) THOR 무GT 재측정 — docs/THOR_NOGT_TASK.md
