# 2026-09-23 실행 시트 — 서버 두 대, 명령 두 줄

이전 지시(`RTX_TASKS_20260907.md` 9-58~9-64)가 여러 절에 흩어지고 정정이 덧붙어 헷갈렸다. **남은 것 전부를 스크립트 두 개로 묶었다.** 각 스크립트는 입력을 먼저 점검하고, 끝난 작업은 건너뛰며(중간에 끊겨도 다시 돌리면 이어서 한다), 끝에 결과 묶음 하나를 만든다.

## 1. 먼저 점검 (GPU 안 씀, 몇 초)
```bash
cd <khronos> && git pull
DRY=1 KC=<khcache 경로> bash scripts/batch/rtx_0923.sh     # 프로6000
DRY=1 KC=<khcache 경로> bash scripts/batch/h100_0923.sh    # H100
```
`✗ 입력 없음:` 줄이 나오면 그 파일/디렉터리를 채운 뒤 다시 점검. 드라이브에서 받을 것은 아래 표 "새 입력" 열뿐이고, tar.gz 를 `KC` 에 두기만 하면 스크립트가 푼다.

## 2. 실행
```bash
KC=<khcache 경로> bash scripts/batch/rtx_0923.sh     # 프로6000 · 약 1~1.5시간
KC=<khcache 경로> bash scripts/batch/h100_0923.sh    # H100 · 약 5시간
```
두 대를 동시에 돌려도 된다(서로 입력을 공유하지 않는다).

## 3. 올릴 것
- 프로6000: `$KC/out_0923/rtx_0923_all.tar.gz`
- H100: `$KC/out_0923/h100_0923_all.tar.gz`

## 무엇을 하나

| 서버 | 번호 | 무엇 | 왜 | 새 입력 (드라이브) |
|---|---|---|---|---|
| 프로6000 | 9-61 | c2set 채택 판정(챔피언 어댑터) | 어제 c2set 결론이 질의 1/3 만 덮은 부분 측정이라 무효. OR 규칙 판정이 여기 걸려 있다 | `rtx_9_61_adopt_pack_c2set_fix.tar.gz` |
| 프로6000 | 9-59 | 새 시뮬 5차분 앵커 등록부 + 기록자리 마진 | 5차분 평가의 마지막 GPU 단계 (API 수정 반영) | 없음 |
| 프로6000 | 9-58 | OWLv2-large 로 소형 물체 재검출 | ② 최대 손실 덩이(작은 물체 미검출 17건) | 없음 (9-54 재사용) |
| H100 | 9-60b | 장면그래프 문맥 판정기 **두 팔 모두 다시** 학습 + 133채·c2set 추론 | 사용자 제안 2·3 의 전제. c2set 의 해로운 ① 채택(7건 중 5건)을 겨눈다 | `rtx_9_61_adopt_pack_c2set_fix.tar.gz` (위와 같은 파일) |
| H100 | 9-64 | ③ 부재 판정기 재학습 2팔 → c3big 타임라인 판정 | 학습된 판정기가 "모르겠다" 로 기울어 ③ 인계를 깎았다(0.62→0.48) | `rtx_9_64_pres.tar.gz` (15:49 판) |

## 이번 정리에서 고친 지시 오류 (이미 돌렸다면 확인)
1. **9-60 합본 명령 인자 순서.** `lora_merge_sets.py` 는 **출력이 첫 인자**인데 지시에는 `--out` 을 붙였다. 그대로면 실패한다. 그래서 ctx0 이 어떤 데이터로 학습됐는지 확인할 수 없어, 9-60b 는 **두 팔을 새 디렉터리(`out_9_60b`)에서 같은 데이터로 다시** 학습한다. 받아 둔 `adopt_margin_ctx0_0923.jsonl` 은 비교에 쓰지 않는다.
2. **9-60 추론 반복문의 변수 확장.** `ctx$CTX_0922` 는 bash 가 `$CTX_0922` 로 읽어 두 팔이 한 파일에 덮어쓴다. 스크립트는 `${CTX}_` 로 쓴다.
3. **9-64 균형 방식.** 3라벨을 그대로 `--balance` 하면 "모르겠다"(187행)가 10배 복제돼 오히려 그쪽으로 기울 수 있다. P1 은 **yes·no 만 같은 수로 맞추고 unsure 는 원래 187행 그대로**, P2 는 **unsure 를 빼고 yes·no 만** 균형 학습으로 바꿨다.


---
## 20:40 갱신 — 중간 결과 반영과 남은 것

받은 스냅샷(`rtx_0923_snapshot_20260923_2033`)에서:
- **9-61 · 9-58 · 9-59 완료.** M2 에서 분석했다(§166-110 OR 규칙 기각 확정, §166-111 큰 검출기 이득 없음, 새 시뮬 5차분 평가 시작).
- **9-61 이 처음에 `KeyError: 'VERIFY_JSONL'` 로 죽은 원인**: `lora_adopt_infer.py` 가 PACK 모드에서도 그 변수를 요구했다. 고쳐서 푸시(PACK 모드면 필요 없음).
- **9-60b 는 프로6000 GPU0 에서 진행 중**(ctx0 학습 끝 · ctx1 학습 1,110/1,826 스텝). 그대로 두면 된다. 끝나면 추론 4건(133채·c2set × 두 팔)까지 자동으로 간다.
- **9-64 는 입력 점검에서 멈춘다**: `rtx_9_63_tl/pack/timeline_prep_sel.jsonl` 이 프로6000 에 없다(9-63 은 H100 에서 돌렸다). 둘 중 하나:
  1. 드라이브의 `rtx_9_63_tl.tar.gz`(513 MB)를 프로6000 `KC` 에 두고 `KC=... bash scripts/batch/h100_0923.sh` 를 **9-60b 가 끝난 뒤** 다시 돌린다(끝난 9-60b 는 건너뛰고 9-64 만 돈다).
  2. 또는 H100 에서 `KC=<H100 khcache> bash scripts/batch/h100_0923.sh` — 거기엔 9-63 묶음이 이미 있다. 이 경우 9-60b 도 거기서 다시 돌기 시작하므로, H100 에서는 9-60b 를 건너뛰도록 `SKIP_960B=1` 을 붙인다.


---
## 9/24 00:30 갱신 — 프로6000 에 9-66 추가 (다시 돌리면 끝난 것은 건너뛰고 9-66 만 돈다)

**왜**: 새 시뮬에서 ② 채택이 0건이던 원인을 찾아 고쳤다(§166-112 — 예시 캐시가 없으면 채택 기하가 통째로 꺼지는 조건). 되살리니 ② 는 4차분 0.33→0.50 · 5차분 0.28→0.44 로 올랐는데, 새 시뮬 구성엔 채택 판정기 필터가 없어서 **① 해로운 채택이 17·42건** 쏟아진다. HSSD 처럼 판정기 마진을 붙이면 된다.

**새 입력(드라이브)**: `rtx_9_66_adopt_pack_newsim.tar.gz` (937 MB) — 새 시뮬 4차분 9,234장 · 5차분 14,318장 (걸은 프레임 전부).
```bash
cd <khronos> && git pull                      # lora_adopt_infer 의 PACK 모드 KeyError 수정 포함
KC=<khcache 경로> bash scripts/batch/rtx_0923.sh
```
→ `out_0923/rtx_9_66_result.tar.gz` 를 드라이브에. 약 40분. 9-60b 가 같은 GPU 에서 돌고 있으면 그 뒤에.
