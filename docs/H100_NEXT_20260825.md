# H100 다음 작업 — 갱신 2026-08-26

## ★★★ 최신: VLM 검증 2차 — 형식 비교 (2026-08-26 저녁)

1차 판정: 크기는 답이 아니다 (4B 0.61/0.72 → 9B 0.68/0.70). 라벨 의존을 없앤
**쌍 대조**("같은 물건인가?" — 등록 크롭 활용) 등 세 형식을 비교한다.

```bash
git pull
# 드라이브 khronos-share/vlmpair_crops.tar.gz (6.7MB) 반입 후
tar xzf vlmpair_crops.tar.gz -C /tmp
MODEL=Qwen/Qwen3.5-9B PAIRS=/tmp/vlmpair \
  python scripts/exp_vlm_verify2.py | tee vlm2_result.txt
```

결과 vlm2_result.txt 를 khronos-share 에. (enable_thinking=False 는 스크립트가 처리)

## (1차 완료) 큰 VLM 검증 (2026-08-26 오후)

검증기가 T1(365건, 최대 병목)의 열쇠인데 4B 는 실사·시뮬 모두 0.60/0.72 로
스펙(0.85/0.85) 미달, DINOv2 유사도도 AUC 0.66 미달. **모델 크기가 원인인지**
이 실행이 가른다.

```bash
git pull
# ① 실사 크롭 160장 반입: work/temp 의 vlmreal_crops.tar.gz 를 서버에 올린 뒤
tar xzf vlmreal_crops.tar.gz -C /tmp
# ② 실행 (게이트 저장소면 export HF_TOKEN=... 먼저)
MODEL=Qwen/Qwen3.5-VL-9B-Instruct CROPS=/tmp/vlmreal THOR_ROOT=data/thor4 \
  python scripts/exp_vlm_verify_hf.py | tee vlm9b_result.txt
# (여유 있으면 MODEL=Qwen/Qwen3.5-VL-32B-Instruct 도 한 번)
```

가져올 것: `vlm9b_result.txt` 한 파일 (몇 줄짜리 텍스트).

## (완료) CLIP 방-노드 캐시

방 인지가 최약체로 확정됐다(프레임 단위 0.431, §81). 개선은 §57 의 CLIP 노드
방출인데 **프레임이 서버에만 있어** 이 캐시는 H100 몫이다. GPU 렌더 불필요 —
순수 추론이라 불칸 없이 돈다.

```bash
git pull
THOR_ROOT=data/thor4 CLIP_PREFIX=/tmp/c4_ STRIDE=4 python scripts/exp_clip_rooms.py
python scripts/harvest.py --root data/thor4 --cache /tmp --prefix c4_ \
  --out harvest_c4_$(date +%m%d).tar.gz
```

가져올 것: `harvest_c4_MMDD.tar.gz` (~250 MB — CLIP 512차원 임베딩).
매핑워크가 없는 thor4 는 텔레포트 맵 프레임을 노드로 쓴다(스크립트가 자동 판단).

---

# (완료됨) 이전 작업 — 2026-08-25

**이 문서 하나만 보면 된다.** H100 쪽에서 할 일과, 끝나면 가져올 파일.

## 할 일 (복사해서 실행)

```bash
cd <repo> && git pull

# ① 앵커 개체 exemplar 캐시 (핵심 — 국소화 0.572→0.641 의 재료)
THOR_ROOT=data/thor4 ACACHE_PREFIX=/tmp/ax_ STRIDE=4 \
  python scripts/exp_anchor_exemplar.py

# ② scene_meta 에 3D 좌표 소급 (배치 증거용 — pos 필드가 예전 판에 없다)
python scripts/export_scene_meta.py --root data/thor4 --force

# ③ 매핑 워크 — 초기 맵 촬영 이동 (걷는 연속 궤적 + GT 뎁스)
python scripts/thor_mapwalk.py --root data/thor4 --gt-depth --platform cloud

# ④ 수확
python scripts/harvest.py --root data/thor4 --cache /tmp \
  --prefix a3_,qc_,ax_ --out harvest_ax_$(date +%m%d).tar.gz --keep-geom
```

⚠️ **H100 에 Vulkan 이 안 잡히면 ②③ 은 실패한다** (둘 다 ai2thor 컨트롤러 필요).
실측: 이 H100 이 그 경우다. **① 과 ④ 만 돌면 충분하다** — ax_ 평가에 ②③ 은
불필요하다(② 좌표는 배치 증거용인데 그 갈래는 임계 경로 아님, ③ 매핑워크는
아이맥 thor3 에서 진행 중). ③ 은 나중에 Vulkan 있는 4090 을 다시 잡을 때 돌린다.

## 가져올 것

`harvest_ax_MMDD.tar.gz` **하나.** (~50 MB 예상. 프레임은 서버에 둔다.)

## 왜

- thor3(16채)에서 **개체앵커+타입가방 결합**이 국소화를 0.572→0.641 로 올렸다
  (GT 프레임 기준 0.638→0.749, 오라클 0.808). thor4(60채×3h)에 적용하려면
  ax_ 캐시가 필요한데 프레임이 서버에만 있다.
- 부재 검출은 thor4 에서 확정됐다(AUC 0.781, n=849). 통합은 0.682 vs 정지 0.672 —
  병목이 검색(이동 물체 0.286)이고, 결합 국소화가 그걸 올릴 첫 재료다.

## 받은 뒤 아이맥에서 (참고 — Claude 가 한다)

```bash
tar xzf harvest_ax_MMDD.tar.gz -C /tmp/h4x
cp /tmp/h4x/cache/ax_*.npz /tmp/h4/cache/
THOR_ROOT=data/thor4 A3_PREFIX=/tmp/h4/cache/a3_ QC_PREFIX=/tmp/h4/cache/qc_ \
  AX_PREFIX=/tmp/h4/cache/ax_ GTFRAME=0 python scripts/eval_anchor_instance.py
# → 결합 국소화를 통합 파이프라인(eval_integrated)에 이식해 재평가
```

## 진행 상황 스냅샷 (2026-08-25)

| 능력 | 상태 | 수치 |
|---|---|---|
| 프레임 선택 (exemplar) | ✅ | 정밀도@5 0.793 |
| 국소화 (결합) | ✅ 돌파 | 실전 0.641 (thor3) |
| 부재 검출 (앵커 게이팅) | ✅ 확정 | AUC 0.781 (thor4) |
| 통합 vs 정지 지도 | ⚠️ 근소 | 0.682 vs 0.672 |
| 검색 @3시간 규모 | ❌ 병목 | 이동 0.286 ← ax_ 로 공략 |
| 초기 맵 구축 | 🔄 진행 | 매핑워크 16채 완료, 구축 스크립트 작성 중 |
