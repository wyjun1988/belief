# 새 시뮬레이터 개선 요청 — 실험 근거 정리 (2026-09-10)

대상: AIUE vol8 두 집(01·02) × 4세션(01_unchanged·02_relocated·03_absent·11_scan) = 6채 실행 결과(`docs/NEWSIM_20260909.md`).
**되는 것부터**: 형식·좌표·방 상자·프레임별 GT 는 우리 파이프라인에 그대로 들어간다(단 §1 의 스캔 영상 프레임 어긋남은 반드시 고쳐야 한다). 임베딩 카메라방 GT 일치 0.87(HSSD 0.81), 스캔 포즈 지도 재투영 1.6 px, 라이브 PnP 등록 68%, ③ 인계 경로 동작(camera 1/1). 아래는 **성능을 읽을 수 있게** 만들기 위한 요청이다. 순서 = 효과 순.

## 1. 스캔 영상 ↔ 주석 프레임 어긋남 (① 손실의 절반 — 2026-09-10 확정, "렌더 품질" 은 원인이 아니었다)

| 근거 | 값 |
|---|---|
| vol8_02 `11_ego_coverage_scan/ego.mp4` | **7,332장** (244.4 s) — `annotations.jsonl` 9,000행, `video_frame` 0..8999 그대로 |
| 어긋남 구조 | 인코더 **정지 2회 × 834행(27.8 s)** (영상 ≈1,245장·≈1,990장 지점). 정지 이후 주석 행 = 영상 프레임 + 834 / + 1,668 |
| 인덱스로 뽑았을 때 GT 박스 정렬률(점수 무관 IoU≥0.3, 가시 박스) | 앞 100 s 0.99 → 이후 **0.05~0.33** (vol8_01 은 전 구간 0.8~1.0) |
| 같은 데이터, 정렬 복원 후 | 큰/중간/작은 재현율 **0.70 / 0.82 / 0.53** (vol8_01 0.61 / 0.58 / 0.27) — vol8_02 가 오히려 낫다 |
| 디노이즈·½해상도·감마 5종 전처리 | 재현율 변화 **±0.02 이내**(오히려 소폭 하락) — 점 잡음·과노출은 검출에 영향 없음 |
| 정렬 복원 후 사슬 재실행, vol8_02 ① 기록 | **0.352 → 0.581** (6채 합산 0.464 → 0.560) — 데이터 한 줄 안 바꾸고 프레임만 맞춘 결과 |

vol8_02 의 "화면이 자글자글하다·큰 물체를 못 잡는다" 는 **영상과 주석이 100 s 이후 다른 시점을 가리키고 있어서** 생긴 착시였다(정지 구간 전의 vol8_02 는 재현율 0.63/0.83/0.66). 라이브 세션(1 fps 300장)은 300/300 으로 맞고, 스캔(30 fps)만 어긋난다.
요청: **(a) 영상 기록기가 프레임을 떨어뜨리지 않게**(정지 시 sim 시간도 같이 멈추거나, 떨어뜨린 프레임을 주석에서도 빼기) **(b) `annotations.video_frame` 을 실제 mp4 프레임 번호로**(또는 mp4 를 VFR 로 쓰고 pts 를 주석 timestamp 와 맞추기) **(c) 세션 끝에 `n_video_frames == n_annotation_rows` 자체 점검**.
우리 쪽 임시 복원: `scripts/newsim_align_video.py` (프레임 차분·위상상관 ↔ 주석 카메라 이동, 단조 정렬 DP; 모의 드롭에서 복원 오차 중앙 0행·p90 1행) → `video_align.json` → 변환기가 정지 구간 행을 지도에서 제외. 이건 회피책이지 해법이 아니다.

렌더 자체에 대한 요청은 낮춘다: 노출·잡음은 그대로 둬도 되고, 해상도 상향(1280×960)은 작은 물체(<64 px, 재현율 0.5) 쪽에 여전히 유효하다.

**주석 박스의 가시성**: `annotations.jsonl` 의 bbox 는 벽·문 뒤 물체까지 투영돼 있다(큰 박스 1,253개 중 다른 방 물체 144 + 화면을 가로지르는 퇴화 박스 191 + 구조물 라벨 471). 우리 무GT 실행에는 안 쓰지만, 진단 지표(검출 재현율)를 낼 때 걸러야 하니 **가시(visible) 플래그** 또는 가림 비율을 넣어 주면 좋다.

## 2. 증거 물체는 집 안에서 **타입 유일**해야 한다 (②③ 4건 중 3건 무효)
| 세션 | 물체 | 같은 타입 수 | 우리 질의 |
|---|---|---|---|
| 01_02 relocated | mug_5 | **17** | 제외 |
| 01_03 absent | camera_14 | 1 | ✅ |
| 02_02 relocated | book_I4 | **28** | 제외 |
| 02_03 absent | magazine_10 | **7** | 제외 |

"where is the mug" 는 머그가 17개면 답이 없다. 요청: 증거 물체를 **집 안에서 그 범주가 하나뿐인 것**에서 고른다(camera·headphones·laptop·kettle·toaster 류). 인스턴스 질의("파란 머그")로 넓히려면 인스턴스 속성(색·크기·별칭)을 evidence 에 같이 준다.

## 3. 물체 범주(category) 필드
`class` 가 자산명(`AI62_005_book_I4`, `am245_20_wood_17`, `ai vol7 figure 3`)이라 명사만 남기는 휴리스틱을 썼는데 `fks`·`black plastic`·`water`·`glove`·`tile cooker`·`wood` 같은 것이 남고, 이들이 ① 오답에 몰려 있다. 검출기(OWLv2)는 텍스트 질의를 쓰므로 **깨끗한 영어 범주 한 단어~두 단어**(mug, table lamp, coffee maker)가 있어야 한다. 요청: scene_graph.objects 와 entities 에 `category` 필드 추가(가능하면 우리 어휘 목록과 맞춤 — `data/hssd_move.json` 의 타입 목록 참고).

## 4. 집당 이동 수 (통계가 안 나온다)
지금 집 2채 × 세션 3 = ② 2건·③ 2건. HSSD v2 는 133채·이동 188건이다. 요청: **세션당 이동 여러 건**(②③ 를 한 세션에 섞어도 우리 평가기는 처리) 또는 집 수 확대. 최소 ③ 30건은 있어야 인계율의 오차가 ±0.1 안에 든다.

## 5. 이동 시각 명시
evidence 에 `leave_time_s`·`arrive_time_s` 를 적어 달라. 지금은 관계 변화(`in <room>` 제거/추가)로 복원하는데, 02_03 magazine 은 떠남=도착=111 s 로 운반 구간이 없다(순간이동으로 보임). 운반 장면(사람이 들고 이동)이 있어야 ② 의 "재목격"과 ③ 의 "부재"가 자연스럽다.

## 6. 방 라벨 통일
`bed_room` vs `bedroom`, `Balcony1/2` 처럼 집마다 표기가 다르다. 요청: `snake_case 유형` + 인스턴스 번호(`bedroom`, `bedroom.001`) 로 통일. 방 상자(rect_xy)는 그대로 좋다.

## 7. 파일·메타 정합
- vol8_02 스캔 mp4 7,332장 ≠ annotations 9,000행 → §1 (인코더 정지 2회). 세션 끝에 두 수가 같은지 자체 점검을 넣어 달라.
- `agent`·mover 캐릭터가 objects 에 섞여 있다 → `is_agent` 표시 또는 제외.
- scene_graph.json 은 map 당 한 벌이면 충분(세션마다 동일).
- depth_mm·instance_mask 는 무GT 실행에 필요 없다(진단용 거리 GT 사다리에만). 당장은 안 받아도 된다.

## 8. 그대로 두어도 되는 것 (확인됨)
- 스캔 30 fps·300 s: 15장에 1장 써도 지도 600장, 옛 자리를 향한 스캔 프레임 22~70장 — 충분.
- ③ 세션의 확인 방문(이동 후 옛 자리를 향한 프레임 61~66장): 설계대로.
- 카메라 fov 90°·pitch 변화: 규약 확정(x=−Y, z=−X, yaw=ψ+180), 변환기가 처리.
- 방 상자(축 정렬)·visible_regions: 그대로 사용.

## 9. 다음 회차에 우리가 할 일
새 데이터가 오면 (mp4 장수 ≠ 주석 행수인 스캔만 `newsim_align_video.py` →) `newsim_to_gt.py` → `nogt_chain_newsim.sh`(PnP top20·20, MIRROR 1) 한 줄로 6채 기준 3시간이면 표가 나온다. 1·2·3 만 반영돼도 ① 과 ③ 인계 표를 낼 수 있다.
