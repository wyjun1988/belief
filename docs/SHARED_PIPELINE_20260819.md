# 하루-스케일 에고센트릭 씬그래프 메모리 — 시스템 공유 문서

2026-08-19 · khronos v3 기준. 모든 수치는 본 저장소에서 실측한 값이며, 아직 실측하지
않은 설계 항목은 **[설계]** 로 표기한다. 근거 문서: `RESULTS_V2_20260815.md`,
`RESULTS_OWL_20260819.md`.

**한 줄 요약** — 웨어러블 1fps 영상만으로 집의 계층 씬그래프(집-방-가구-물건)를
만들고, 하루치 기록을 가볍게 쌓았다가 비동기로 그래프를 갱신하며, "물건이 지금
어디 있나(belief)"까지 답한다.

---

## 1. 초기 맵·씬그래프 구축 — 처음 한 번, 자세하게

### 1-1. Perception 3종: mono depth · pose · segmentation

| 기술 | 구현 | 핵심 수치 |
|---|---|---|
| **Pose** | 증분 SfM (PnP 등록 + 다시점 삼각측량 + 희소 BA) | 삼각측량 검증(`accept_point`) 없으면 씨앗 BA 재투영 RMS **166,215px** → 검증 후 정상 수렴. 물체 랜드마크 공유 기반 **루프 클로저**(섬 병합·브리지) 포함 |
| **Mono depth** | DA3(Depth Anything 3) + **SfM 포즈로 역깊이 아핀(a,b) 재정렬** | 물체 배치오차 **1.61 m → 0.72 m** (재정렬이 단일 최대 개선). 미터 스케일은 바닥 사전(착용 높이 1.55 m)으로 고정 |
| **Segmentation** | FastSAM(+BotSort 트랙) / OWLv2(오픈보캡 박스) | FastSAM 사전으로 방 서명 **0.971** = GT 사전(0.966)과 동등. OWLv2 존재판정 F1 **0.39~0.43** = CLIP(0.22~0.27)의 **1.7배**, 재현율 상한 0.29→**0.89** |

포즈-깊이 반복 보정: DA3 창을 SfM 랜드마크에 앵커링해 조각별 sim3 로 잇는다.
정보 한계로 ~0.75 m 에서 정체 — 이때 깊이 재정렬(위 1.61→0.72 m)로 우회했다.

### 1-2. 계층 씬그래프: 집 - 방 - 가구 - 물건

| 층 | 방법 | 수치 |
|---|---|---|
| 방 분할 | **3D 궤적 군집**(중력 정렬 → 바닥 투영 → 체류 군집). 이미지 한 장 분류가 아니라 **위치**로 정한다 | 프레임-방 정확도 CLIP 한 장 분류 0.52 → **3D 군집 0.93** (1.9배) |
| 방 서명 | 같이 보이는 정적 물체 집합의 TF-IDF 투표 (포즈·기하 불사용) | ADT 112시퀀스 교차 검증 **0.97** (우연 0.62) |
| 방 이름 | **Qwen 자유 생성** — 방 안 물체 조합을 통째로 주고 이름을 짓게 한다 | decoration 3/4 · Nymeria 부엌 정확 분리. ⚠️ 문법 제약(GBNF)을 걸면 `"json"` 같은 퇴화 출력 — **문법 제약은 단일 토큰 선택에만** |
| 가구 병합 | 카테고리 + 3D 근접(0.6 m) | — |

### 1-3. 열린 어휘: "물체 N" + class 는 attribute

노드는 `물체1, 물체2, …` 이고 클래스(노트북/머그)는 attribute 다. 방 타입·방 개수도
고정하지 않는다. **절제 실측**: 물체 클래스 전부 미지 / 방 타입 전부 미지로 바꿔도
belief top-1/top-3 이 **0.67/1.00 그대로** — 모델이 쓰는 것은 이름표가 아니라
방-가구 소속 구조와 관측 이력이다. 새 물체·새 집 구조가 와도 그대로 돈다.

---

## 2. 1fps 하루 기록 — 가볍게 쌓는다

### 2-1. 공간 1차 필터링 [설계]

GPS/위치로 "지금 어느 씬그래프(집/사무실) 안인가"를 먼저 거른다. 대응 실측:
Nymeria 는 세션들이 **위치 식별자(graph_uid) 하나로 같은 좌표계**에 묶인다 —
같은 집의 11세션이 ±8 m 오차로 정합 없이 한 지도가 됐다. GPS 필터는 이 식별자를
실외에서 만드는 문제로 환원된다.

### 2-2. 프레임은 latent 또는 메타데이터로만

원본 영상은 버리고 프레임당 임베딩(CLIP/SigLIP) 또는 검출 메타데이터만 남긴다.
**층별 혼합이 실측 결론이다** (8개 층 개별 측정, `RESULTS_OWL_20260819.md`):

| 질문 유형 | 지각층 | 근거 |
|---|---|---|
| **사건** — "언제/어디서 그 일이 있었나" (검색·QA) | **CLIP latent** | 검색 hit@5 0.70, OWLv2 섞으면 0.55 로 악화 · QA 0.47→0.38 악화 |
| **상태** — "그 물건이 거기 있나" (지도·belief) | **OWLv2 메타데이터** | 가구 belief top-1 7.5배 · Nymeria belief −22%→+20% (부호 반전) |

둘 다 남기는 것이 맞다 — 아래 예산 안에 둘 다 들어간다.

### 2-3. 저장 예산: 12시간 ≤150 MB

실측 기반 환산 (1fps = 43,200프레임/12h):

| 항목 | 실측 | 12h 환산 |
|---|---|---|
| CLIP 512-d 임베딩 (npz 압축) | 1,951프레임 = 1.92 MB | **≈ 43 MB** |
| OWLv2 검출 메타 (문턱 0.3, ~9개/프레임, 이진 인코딩) | — | **≈ 1~5 MB** |
| (참고) OWLv2 원본 JSON 그대로 | 1,951프레임 = 17.3 MB | 384 MB — 문턱·인코딩 필수 |

**합계 ≈ 50 MB / 12h** 로 150 MB 예산의 1/3. int8 양자화 시 latent 가 절반으로
더 줄어 총 ~30 MB 도 가능하다.

---

## 3. 비동기 씬그래프 갱신 — 충전 중에

### 3-1. 물체를 1fps DB 에서 찾는다

씬그래프의 각 물체 클래스를 질의로 저장된 임베딩/메타를 검색한다.
검색기 수치: 물체·위치 질문 hit@5 **0.70** (오라클 = 선택지 공개 검색과 동률),
인과 마스크(질의 시각 이전만)로 전체 hit@5 0.34→**0.47**.

### 3-2. Extended query — 주변 물체로 질의를 넓힌다

Qwen 이 키워드 + 이웃(같이 있을 물체·장소)을 JSON 으로 추출하고, PMI 동시출현
그래프가 문맥 이웃을 보강한다. 확장 질의로 검색 시 물체·위치 hit@5 **0.55 → 0.70**.

### 3-3. 갱신 방법: voting (실측) / LLM 키프레임 [설계]

관측을 방 단위로 **누적 투표**한 사전분포가 곧 갱신이다 — Nymeria 세션 간
belief 에서 이 사전분포가 최빈방 대비 **+20%** (0.632 vs 0.526)로 유일하게
기준선을 넘었다. 두 가지 실측 조건이 붙는다:

- **근접 게이트가 전제조건.** 방을 착용자 위치로 정하므로 문 너머 약한 검출이
  지도를 오염시킨다. 검출 점수 문턱 0.35 에서 고정 가전의 세션 간 방 일치도
  0.53→**0.93** (stove 0.50→1.00), belief 0.378→0.632.
- **동의어 확장은 채택하지 않는다.** 재현율은 오르지만(0.89→0.96) 같은 폭의
  오검출이 누적층을 오염 — belief 0.644→0.53 으로 악화 실측.

키프레임을 모아 LLM 으로 갱신하는 변형은 [설계] — 현재는 투표로 충분했다.

### 3-4. 부재 증거 — "없어졌다"를 검색으로 확인

**부재 질의 = extended query − 키워드.** 키워드를 넣어 검색하면 물건이 보이는
프레임만 올라와(생존 편향) 부재가 관측 불가능하다. 문맥만으로 "있어야 할 장소"의
프레임을 찾고, 그 안에서 키워드 존재도를 재야 "그 자리에 없다"가 보인다.

실측 (ADT 2시퀀스, GT 이동구간 채점): 이동 물체 z 하락 +1.21 vs 정적 −0.12,
**AUC 0.761 · 순열검정 p = 0.014**. 판정 전제는 문맥 게이트 — 그 장소를 실제로
본 프레임이 없으면 부재를 주장하지 않는다(증거의 부재 ≠ 부재의 증거).
지각층 교체 이득은 없음(CLIP 0.761 / OWLv2 0.752 동급).

---

## 4. 사용자 QA — 라우팅 후 VQA 또는 belief

질문이 오면 Qwen 이 **키워드·이웃·질문 유형(vqa/belief)을 한 번에** 판정하고
(문법 JSON, thinking off, 데모 3/3 정확), 3의 질의로 그래프를 갱신한 뒤 답한다.

| 경로 | 방법 | 수치 |
|---|---|---|
| 단순 VQA | 검색 상위 프레임 → Qwen3.5-4B VLM (llama.cpp, GPU) | 물체·위치 정답률 **0.62** (strict 66문항 · 영상 없음 0.19 · 무작위 0.33) |
| belief | home-jepa 확률 모델 (방-가구 구조 + 관측 이력) | 아래 |

### 4-1. belief 는 미관측 영역 질문 — 성립 조건까지 실측했다

"노트북이 거실에 있다가 없어졌다" 류. 부재 증거가 없으면 LLM 폴백.

| belief 층 | 판정 | 수치 |
|---|---|---|
| **세션 간** (벽 있는 집, Nymeria) | **성립** | 최빈방 대비 **+20%** — 유일한 기준선 초과 |
| **가구 단위** | 성립(개선 중) | CLIP 무작위 수준(0.02) → OWLv2 top-1 **0.15**/top-3 0.27 (무작위 0.04/0.13). 단 최빈가구 0.24 미달 |
| 방 단위 (개방형 원룸, SuperMemory) | **접음** | 어떤 사전분포 가중도 최빈방(0.69) 미달 — α 스윕이 최빈방에서 고원, 순이득 −20 |

**방 단위 belief 는 방이 실제로(벽으로) 구분될 때만 성립한다.** 개방형 공간의
'방'은 사람의 기능 구획이라 물체 분포로 경계를 복원할 수 없다 — 모델 실패가
아니라 과제의 전제 조건. 두 데이터셋이 독립적으로 같은 결론을 냈다.

---

## 5. 사용 기술 총괄 (수치 요약)

| 기술 | 상태 | 핵심 수치 |
|---|---|---|
| **SfM** (증분·PnP·희소 BA) | 실측 | 삼각측량 검증 없으면 RMS 166,215px — 검증이 지도 오염을 막는다 |
| **루프 클로저** | 실측 | 물체 랜드마크 공유 기반 섬 병합·브리지 (합성 12종 포함 테스트 38개 통과) |
| **물체 기준 pose·위치 반복 보정** | 실측 | DA3 앵커링 ~0.75 m 정체 → SfM 포즈로 깊이 재정렬 **1.61→0.72 m** · 바닥 사전 미터 스케일 |
| **지각층** (CLIP↔OWLv2) | 실측 | F1 1.7배 · 재현율 상한 0.29→0.89 · **층별 혼합**(사건=CLIP, 상태=OWLv2)이 결론 |
| **데이터셋 확보** | 완료 | ADT 2시퀀스(GT 세그·깊이·포즈) · SuperMemory 5세션 10,891프레임+QA 73문항 · Nymeria Loc_49 11세션 **단일 좌표계** |
| **세션 간 지도 합치기** | 실측 | Aria Gen2 교차 세션 정합 **1.168→0.125 m** (<0.3 m 비율 7%→81%) · Nymeria 11세션 graph_uid 정합 불요 |
| **기억 검색기** (1fps 검색) | 실측 | 확장질의 hit@5 물체·위치 **0.70** = 오라클 동률 · 인과 마스크 +0.13 |
| **부재 증거** | 실측 | AUC **0.761** (p=0.014) · 부재질의 = 확장질의 − 키워드 |
| **belief 모델** (home-jepa) | 실측 | 열린 어휘 비용 0 · 세션 간 +20% · 성립 조건 = 벽 |
| **방 명명·질의 계획** (Qwen) | 실측 | 방 이름 3/4 · 라우팅 3/3 · 문법 제약은 단일 토큰에만 |
| **연산 분담** | 운용 | 검출 MBP(M1 Pro MPS) 0.57s/장 = 아이맥 CPU 의 **16배** (텍스트 임베딩 1회 캐시로 추가 1.6배) |
| GPS 공간 필터 · SigLIP 교체 · LLM 키프레임 갱신 | [설계] | 미실측 — 2-1·2-2·3-3 참조 |

### 남은 병목 (다음 단계)

1. **개방형 공간의 방 분할** — 두 데이터셋이 독립적으로 지목한 최종 병목.
2. 가구 belief 를 최빈가구(0.24) 위로 — top-3 은 이미 무작위의 2배.
3. 부재 증거 표본 확대 — 현재 이동 물체 6~8개(ADT 한계), 이동 많은 데이터 필요.
