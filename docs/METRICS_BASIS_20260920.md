# 지표 기준 대조표 — **움직일 수 있는 물건(MOVABLE_MIN=0.1)** 이 정식 기준 (2026-09-20)

2026-09-19 사용자 결정: *"모든 평가는 움직일 수 있는 것으로 한다. ①도 그냥 어디 있었지 하고 물어볼 수 있다."*
이 표 밖의 문서에서 **기준이 명시되지 않은 수치는 옛 기준(전부)일 수 있으니 인용하지 말 것.** `bench.sh` 기본이 `MOVABLE_MIN=0.1` 이 됐고, 이동성 표에 없는 타입이 절반을 넘으면 `eval_online` 이 중단한다(새 시뮬레이터는 `MOVABLE_MIN=0` 명시).

## 1. 헤드라인 — 지표별 정식 출처 (구성비가 달라 **총점은 쓰지 않는다**)

| 지표 | 출처 | 옛 기준(전부) | **새 기준(움직이는 것)** | 표본 변화 |
|---|---|---|---|---|
| **① 안 움직인 물건의 방** | HSSD 133채 | 0.805 (n=837) | **0.720** (n=143) | 812건이 냉장고·욕조 등 붙박이 → 제외 · 9/21 PnP 완화 후 0.713→0.720 |
| **② 이동 후 재촬영** | HSSD 133채 | 0.416 (n=137) | **0.453** (n=137) | 9/21 PnP 완화(등록 0.61→0.83)로 0.416→0.453 · 채택정답 .93 |
| **③ 부재 → 인계** | c3big 69채 | 0.620 (n=71) | **0.620** (n=71) | 불변 — 원래 움직이는 물건 |
| ③ 무인계 준수 (확인기회X) | c3big | 0.55 (n=58) | **0.55** | 불변 |
| ① (③ 셋에서) | c3big 69채 | 0.802 (n=464) | **0.727** (n=77) | 붙박이 제외 |
| 새 시뮬레이터 4차분 ① | 10채 | — | **0.722** (n=295, `MOVABLE_MIN=0`) | 벤더가 이미 휴대물품만 골랐다 — 표 적용 시 250건 오제외(9/19 "0.632" 무효) |

모두 현재 챔피언 구성(채택 필터 `all_full` · `INST_SEL=priorvote` · **포즈 `pose_all_relax`(9/21)** · c3big 은 `absv_sel`).

## 2. 옛 기준으로 내렸던 결론 — 새 기준 재검증

| 결론 (§) | 옛 기준 근거 | 새 기준 재측정 | 판정 |
|---|---|---|---|
| 채택 필터 `all_full` 채택 (§166-88) | 총 0.726→0.732 | 총 0.508→0.518 · ① 0.685→0.699 · ② 0.401→0.409 | **유지** (§166-92) |
| 어댑터 10종 순위 (§166-88) | all_full 1위 | all_full 1위 (allhn·lr3 0.514 · r32 0.511) | **유지** |
| `INST_SEL=priorvote` (§166-93) | — (새 기준에서 처음 측정) | HSSD ① 0.699→0.713 · c3big 0.688→0.727 | **채택** |
| c3big `sel` 인스턴스 통일 (§166-83) | 총 0.623→0.628 "모든 축 공짜" | ① 0.701→**0.727** · ③ 인계 0.634→**0.620** · 무인계 0.59→0.55 | **수정: 맞바꿈.** 유지하되 "공짜" 아님 |
| 기록자리 판정기 HSSD 무효 (§166-90) | 25채 상쇄(4:4) | 25채 `PLACE_W` 1/2/4 전부 변화 0 (n=26) | **유지** (§166-93) |
| 후보 상한·재군집 문턱 (§166-93/94) | — | k5·k16·th0.30 전부 ①↔② 맞바꿈 | **기각 유지** |
| 방 라벨 구성점 투표 (§166-93) | — | HSSD +0.014 / c3big −0.013 | **기각 유지** |
| ① 은 물체 크기에 좌우 (§166-91) | 전부 기준 표 | 기술 통계라 그대로 유효 (큰 가구 0.838 / 작은 0.705 / 4차분 0.576) | 라벨만 명시 |

## 3. 새 기준에서 새로 확인된 것 (9/20)
- **① 오답 43건** = 인스턴스 선택 19 · 군집 위치 12 · 방 라벨 10 · 거짓채택 2 · **검출 실패 0** (§166-93)
- **오답의 76% 가 `stand`(0.455)·`floor lamp`(0.705)**. `stand` 는 HSSD 카테고리가 실제로 TV 장(31/33) — 질의어 별칭 파일럿 진행 중 (§166-94)
- **② 병목 = 포즈 커버리지**: 진짜 손실 42건 중 23건이 채택 창 3장 중 포즈 <2. 검증기 문턱·창 크기·카메라방 투표는 전부 무효 또는 맞바꿈 (§166-95). HSSD PnP 완화 재실행 예약

## 4. 인용 규칙
1. 수치 옆에 **기준과 표본 n 을 항상 같이** 적는다.
2. 데이터셋 간 비교는 같은 지표끼리만(① 은 ① 과). 총점 비교 금지.
3. 새 시뮬레이터는 `MOVABLE_MIN=0` — HSSD 이동성 표가 적용되지 않는다.
4. 옛 문서(§166-88 이전, STATUS_20260919 상단)의 총점 0.732·① 0.806 은 **전부 기준** — 새 기준 수치와 나란히 두지 않는다.
